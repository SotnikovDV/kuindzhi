# Пример переменных окружения (не содержит секретов)
#
# Скопируйте этот файл в `.env` (локально/на сервере) и заполните значения.
# ВАЖНО: `.env` НЕ должен попадать в GitHub.

# Порт приложения
PORT=3400

# Perplexity API key (никогда не коммитьте реальный ключ)
PERPLEXITY_API_KEY=YOUR_KEY_HERE

